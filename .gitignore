# Test and tool output
*.test
*.out
coverage*.txt
/tmp/
# Fuzz caches live in GOCACHE; checked-in seeds live in testdata/fuzz and are NOT ignored.
# A local workspace over the core checkout is never committed
go.work
go.work.sum
